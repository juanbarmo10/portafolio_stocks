"""Panel pages, one per question of the checklist (CLAUDE.md section 2).

Streamlit executes these by path through ``st.Page``; the package exists so the shared
helpers in ``app.data`` import the same way everywhere.
"""
